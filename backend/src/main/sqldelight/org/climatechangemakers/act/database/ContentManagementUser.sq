CREATE TABLE content_management_user (
  user_name VARCHAR NOT NULL PRIMARY KEY,
  password_sha_512 VARCHAR NOT NULL
);

userWithPasswordHashExists:
SELECT EXISTS(
  SELECT * FROM content_management_user
  WHERE user_name = :userName AND password_sha_512 = :passwordSha512
);

