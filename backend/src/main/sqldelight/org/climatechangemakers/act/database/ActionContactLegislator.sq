CREATE TABLE action_contact_legislator(
  id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
  email VARCHAR NOT NULL,
  contacted_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
  issue_id BIGINT NOT NULL REFERENCES issue ON DELETE CASCADE,
  contacted_bioguide_id VARCHAR NOT NULL REFERENCES member_of_congress(bioguide_id)
);