CREATE TABLE action_initiate(
  -- TODO(kcianfarini) update this to CITEXT when SQLDelight supports that grammar.
  -- TODO(kcianfarini) This doesn't cause a problem in the interim, but would be nice to have the proper type represented.
  -- TODO(kcianfarini) related issue: https://github.com/AlecStrong/sql-psi/issues/288
  email VARCHAR NOT NULL,
  initiated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

insert:
INSERT INTO action_initiate(email)
VALUES(:email);

